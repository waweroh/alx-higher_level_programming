#!/usr/bin/python3
"""
Module containing the function is_same_class()
"""


def is_same_class(obj, a_class):
    """return true if object is the same class, otherwise false"""
    return (type(obj) == a_class)
